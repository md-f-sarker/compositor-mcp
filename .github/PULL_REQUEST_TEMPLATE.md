## Summary

What does this change and why?

## Testing

- [ ] `npm run check` passes (typecheck, tests, parity, generated docs)
- [ ] Capability changes update the registry, Swift router, tests and parity docs together

## Native testing

**Upstream Compositor commit tested against:** `<full SHA of the Compositor checkout used for native/live testing, or "not run — TypeScript-only change">`

Any PR that touches `compositor/`, the installer, or operation behaviour must record the upstream commit it was verified against — see CONTRIBUTING.md.
